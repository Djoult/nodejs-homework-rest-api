export const subscriptionList = ["starter", "pro", "business"];

export const BEARER = "Bearer";
export const emailRegexp = /^[\w-\.]+@([\w-]+\.)+[\w-]{2,4}$/;
export default { subscriptionList, BEARER, emailRegexp };
